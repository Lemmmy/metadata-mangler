export const appName = "Metadata Mangler";
export const appId = "metadata-mangler";
export const appContact = "mm@drew.contact";

export const makeAppTitle = (title: string) =>
  title ? `${title} | Metadata Mangler` : "Metadata Mangler";
